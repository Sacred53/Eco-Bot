const Discord = require("discord.js");
const errorm = require("./errorm.json");

const bl = require("./list.json");
module.exports.run = async (bot, message, args) => {
}

module.exports.help = {
    name: ""
};